{
	"name": "developer-of-the-hour",
	"version": "0.0.1",
	"private": true,
	"author": "PatrickNausha",
	"scripts": {
		"dev": "parcel index.html",
		"build": "rm -rf ./dist && NODE_ENV=production parcel build index.html --public-url ./",
		"deploy": "gh-pages -d dist"
	},
	"devDependencies": {
		"gh-pages": "^3.0.0"
	},
	"dependencies": {
		"three": "^0.118.3"
	}
}
